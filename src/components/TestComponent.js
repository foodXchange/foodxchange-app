import React from 'react';

const TestComponent = () => {
  return (
    <div style={{ padding: '20px', textAlign: 'center' }}>
      <h1>? FoodXchange is Working!</h1>
      <p>If you can see this, the syntax errors have been fixed.</p>
      <p>Navigate to the features using the menu above.</p>
    </div>
  );
};

export default TestComponent;
